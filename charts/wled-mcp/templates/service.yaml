apiVersion: v1
kind: Service
metadata:
  name: {{ include "wled-mcp.fullname" . }}
  labels: {{- include "wled-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "wled-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
